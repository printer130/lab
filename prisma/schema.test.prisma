generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = "postgresql://postgres:a3b8pn0o0pd0l4pa@db.qtucoqsygwbjjhykeyzo.supabase.co:5432/postgres?schema=public"
}

model User {
  firstName String
  lastName  String
  email     String  @unique
  isAdmin   Boolean @default(false)

  @@id([firstName, lastName, isAdmin])
  @@index([firstName, isAdmin])
}
