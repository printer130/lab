// import { PrismaClient } from '@prisma/client'
// const prisma = new PrismaClient()

export default async function handle (req, res) {
  // const r = await prisma.users.findUnique({
  //   where: {
  //     id: 1
  //   }
  // })
  // console.log(r)
  // return r.json()
  return res.json({
    asd: 'asd'
  })
}
